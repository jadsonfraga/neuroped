# NeuroPed — Bug hunt / caça às bruxas

Data: 2026-06-07

## Corrigido nesta rodada

### 1. Relatório clínico

Arquivo: `client/src/components/ClinicalReport.tsx`

Correções:

- removido e-mail antigo `jadsonfraga@hotmail.com`;
- definido e-mail institucional `drjadsonfraga@proton.me`;
- removidos `CRM-BA`, `RQE 14499` e `RQE 13119` do cabeçalho impresso;
- assinatura institucional reduzida para `CRM-PE 25227 · RQE 17756`;
- mantidos PDF/impressão com todas as respostas;
- mantido WhatsApp/Zap com fallback por limite de URL;
- mantido `escapeHtml` no HTML de impressão;
- mantido bloqueio de PDF quando `reportReady` é falso.

### 2. Página pública Sobre

Arquivo: `client/src/pages/sobre.tsx`

Correções:

- removido `CRM-BA` da página pública;
- removidos RQEs extras;
- mantido apenas `CRM-PE 25227` e `RQE 17756`;
- atualizado telefone para `(87) 9 9109-7371` no link `tel:+5587991097371`;
- mantido e-mail `drjadsonfraga@proton.me`;
- atualizado endereço para `Rua Raimundo Lacerda, nº 001 — Bairro São José — CEP 56302-470`;
- aplicado uso de imagens com `object-contain`/`no-zoom-media` para reduzir cortes no carrossel.

### 3. Auditoria institucional

Arquivo criado: `scripts/guards/audit-institutional-identity.mjs`

A auditoria reprova arquivos públicos/centrais se encontrar:

- `CRM-BA`;
- `23384`;
- `14499`;
- `13119`;
- `jadsonfraga@hotmail.com`;
- `32013648`;
- `Cardoso de Sá`;
- `Av. Cardoso`.

Também exige no relatório clínico:

- `drjadsonfraga@proton.me`;
- `CRM-PE 25227 · RQE 17756`.

### 4. Scripts

`package.json` agora inclui:

- `npm run audit:identity`;
- `npm run verify` com auditoria institucional.

### 5. CI

Workflow `Verify NeuroPed` atualizado para rodar:

- TypeScript;
- catálogo;
- acesso;
- identidade institucional;
- assets;
- testes clínicos;
- build.

Workflow GitHub Pages também passa por `audit:identity` antes do build/deploy.

## Observação operacional

A tentativa de editar o workflow Cloudflare completo foi bloqueada pelo conector por envolver bloco de secrets/deploy. A proteção institucional já está no `verify` e no GitHub Pages. Próximo ajuste seguro, se necessário, deve ser feito em ambiente local ou via PR pequeno sobre o workflow Cloudflare.

## Rotas prioritárias para checagem

- `/sobre`
- `/mchat` ou qualquer escala finalizada com `ClinicalReport`
- `/qualidade`
- `/filtro`
- `/pant`
- `/pacientes`

## Veredito

Foram corrigidos bugs de identidade institucional em pontos públicos/centrais e criada catraca para reduzir regressão futura.
