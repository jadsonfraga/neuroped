-- NeuroPed D1 production seed intentionally empty.
-- Demo content is supplied in-memory by the UI/API fallback and must never be
-- persisted as clinical plaintext after the encrypted-at-rest migration.
SELECT 1;
