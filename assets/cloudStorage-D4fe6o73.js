const STORAGE_KEY="neuroped_demo_local_v1";
const EMPTY_STATE={patients:[],scale_results:[],reminders:[],appointments:[],portal_documents:[],app:"neuroped-dr-jadson",mode:"local-demo"};
let cache=null;
function cloneState(state){return JSON.parse(JSON.stringify(state||EMPTY_STATE))}
function normalizeState(state){const data=state&&typeof state==="object"?state:{};return{...EMPTY_STATE,...data,patients:Array.isArray(data.patients)?data.patients:[],scale_results:Array.isArray(data.scale_results)?data.scale_results:[],reminders:Array.isArray(data.reminders)?data.reminders:[],appointments:Array.isArray(data.appointments)?data.appointments:[],portal_documents:Array.isArray(data.portal_documents)?data.portal_documents:[]}}
function safePatient(input={}){const patient={name:String(input.name||"").trim()};if(input.birthDate)patient.birthDate=input.birthDate;if(input.notes)patient.notes=String(input.notes).slice(0,1500);if(input.cid)patient.cid=String(input.cid).trim().toUpperCase();return patient}
function safePayload(input={}){return cloneState(input)}
async function fetchAllData(force=false){if(cache&&!force)return cache;try{const raw=localStorage.getItem(STORAGE_KEY);cache=normalizeState(raw?JSON.parse(raw):EMPTY_STATE)}catch{cache=cloneState(EMPTY_STATE)}return cache}
async function persist(state){cache=normalizeState(state);try{localStorage.setItem(STORAGE_KEY,JSON.stringify(cache))}catch(error){console.error("[localStorage] save failed:",error);throw error}}
async function listPatients(){return[...(await fetchAllData()).patients].sort((a,b)=>a.createdAt>b.createdAt?-1:1)}
async function getPatient(id){return(await fetchAllData()).patients.find(patient=>patient.id===id)}
async function createPatient(input){const state=await fetchAllData(true);const patient={...safePatient(input),id:`pat_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`,createdAt:new Date().toISOString()};state.patients.push(patient);await persist(state);return patient}
async function updatePatient(id,input){const state=await fetchAllData(true);const index=state.patients.findIndex(patient=>patient.id===id);if(index<0)return null;state.patients[index]={...state.patients[index],...safePatient(input),id};await persist(state);return state.patients[index]}
async function deletePatient(id){const state=await fetchAllData(true);const before=state.patients.length;state.patients=state.patients.filter(patient=>patient.id!==id);state.scale_results=state.scale_results.filter(result=>result.patientId!==id);if(state.patients.length===before)return false;await persist(state);return true}
async function listScaleResults(){return[...(await fetchAllData()).scale_results].sort((a,b)=>a.createdAt>b.createdAt?-1:1)}
async function listScaleResultsForPatient(patientId){return(await listScaleResults()).filter(result=>result.patientId===patientId)}
async function saveScaleResult(input){const state=await fetchAllData(true);const result={...safePayload(input),id:`res_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`,createdAt:new Date().toISOString()};state.scale_results.push(result);await persist(state);return result}
async function deleteScaleResult(id){const state=await fetchAllData(true);const before=state.scale_results.length;state.scale_results=state.scale_results.filter(result=>result.id!==id);if(state.scale_results.length===before)return false;await persist(state);return true}
async function listReminders(){return[...(await fetchAllData()).reminders].sort((a,b)=>a.dataHoraConsulta>b.dataHoraConsulta?1:-1)}
async function createReminder(input){const state=await fetchAllData(true);const reminder={...safePayload(input),id:`rem_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`,criadoEm:new Date().toISOString()};state.reminders.push(reminder);await persist(state);return reminder}
async function updateReminder(id,input){const state=await fetchAllData(true);const index=state.reminders.findIndex(reminder=>reminder.id===id);if(index<0)return null;state.reminders[index]={...state.reminders[index],...safePayload(input),id};await persist(state);return state.reminders[index]}
async function deleteReminder(id){const state=await fetchAllData(true);const before=state.reminders.length;state.reminders=state.reminders.filter(reminder=>reminder.id!==id);if(state.reminders.length===before)return false;await persist(state);return true}
async function listAppointments(){return[...(await fetchAllData()).appointments].sort((a,b)=>{const left=`${a.date||""}T${a.time||""}`;const right=`${b.date||""}T${b.time||""}`;return left<right?-1:left>right?1:0})}
async function createAppointment(input){const state=await fetchAllData(true);const appointment={...safePayload(input),id:`apt_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`,criadoEm:new Date().toISOString()};state.appointments.push(appointment);await persist(state);return appointment}
async function deleteAppointment(id){const state=await fetchAllData(true);const before=state.appointments.length;state.appointments=state.appointments.filter(appointment=>appointment.id!==id);if(state.appointments.length===before)return false;await persist(state);return true}
async function publishPortalDocument(){console.warn("[Portal Familias] Publicacao desativada na versao estatica por seguranca/LGPD.");return null}
async function findPatientByCpf(){return null}
export{createAppointment,createPatient,createReminder,deleteAppointment,deletePatient,deleteReminder,deleteScaleResult,fetchAllData,findPatientByCpf,getPatient,listAppointments,listPatients,listReminders,listScaleResults,listScaleResultsForPatient,publishPortalDocument,saveScaleResult,updatePatient,updateReminder};
